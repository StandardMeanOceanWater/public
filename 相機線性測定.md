---
url: https://hackmd.io/JdDyVaZgSiaJ_c3m_Wpeuw
title: 相機線性測定
lastSync: 2026-03-11T21:20:51.028Z
---
https://www.photonstophotos.net/Charts/PTC.htm
這裡面有別人測的，但是很可能不能用
## 1 廠商會不會提供 linearity curve？

通常 **不會提供完整曲線**。

相機或感測器資料表通常只會給：

- **Full well capacity**
    
- **Gain**
    
- **Read noise**
    
- **Dynamic range**
    

很少提供：

```
ADU vs exposure linearity curve
```

原因：

1. 每台相機個體差異
    
2. DSLR 會有 firmware 處理
    
3. RAW pipeline 不完全公開
    

因此做測光時：

**最好自己量測 linearity。**

這在變星測光是標準流程。

---

## 2 什麼是 linearity curve

理想情況：

```
曝光時間 ×2
→ ADU ×2
```

也就是：

```
ADU ∝ exposure time
```

但實際上：

- 接近飽和時會變非線性
    
- 很低訊號時受 read noise 影響
    

所以會出現：

```
      /
     /
    /
   /
  /
 /
---------
exposure
```

後段會彎曲。

---

## 3 為什麼測光需要它

測光假設：

```
flux ∝ counts
```

如果相機非線性：

```
亮星會被低估
```

所以一般要求：

```
peak < 50% full well
```

保守一點：

```
30–40%
```

---

## 4 學生可以做的自測方法（簡單版）

需要：

- 穩定光源
    
- 均勻平面（白牆 / flat panel）
    

鏡頭 **完全不動**。

---

### Step 1 固定 ISO

例如：

```
ISO 800
```

不要改 ISO。

---

### Step 2 固定光源

可以用：

- 平板螢幕
    
- tracing panel
    
- flat box
    

重點：

```
光不能變
```

---

### Step 3 拍一系列曝光

例如：

```
0.5 s
1 s
2 s
4 s
8 s
16 s
```

每個曝光拍 **3 張**。

---

### Step 4 轉成 FITS

用 NINA 或 raw converter。

---

### Step 5 量測中央區域 ADU

取中央例如：

```
200 × 200 pixels
```

計算：

```
median ADU
```

---

### Step 6 畫圖

X 軸：

```
exposure time
```

Y 軸：

```
ADU
```

理想應該是一條直線。

---

## 5 判斷線性範圍

例如結果：

```
0.5 s → 2000 ADU
1 s   → 4000
2 s   → 8000
4 s   → 16000
8 s   → 30000   ←開始偏離
```

那線性範圍就是：

```
ADU < ~25000
```

之後測光就限制：

```
star peak < 25000
```

---

## 6 自動生成 linearity curve 的 Python

學生可用：

```python
import numpy as np
import matplotlib.pyplot as plt
from astropy.io import fits
from pathlib import Path

folder = Path("linearity_test")

exposures = []
adu = []

for f in sorted(folder.glob("*.fits")):

    with fits.open(f) as hdul:
        data = hdul[0].data
        exp = hdul[0].header["EXPTIME"]

    center = data[2000:2200,2000:2200]

    exposures.append(exp)
    adu.append(np.median(center))

plt.scatter(exposures, adu)

plt.xlabel("Exposure time (s)")
plt.ylabel("ADU")

plt.show()
```

---

## 7 對你們設備的實際建議

望遠鏡：

Vixen R200SS Telescope

典型拍攝下：

變星測光通常控制：

```
peak ADU
20000–30000
```

通常都在線性區。

---

## 8 一個很多學生會犯的錯誤

不要用：

```
JPEG
```

或

```
demosaic RAW
```

一定要用：

```
RAW → FITS
```

否則相機內部會：

```
tone curve
gamma
highlight compression
```

linearity 會完全失真。

---

如果需要，我也可以給你一個 **完整的 120 行 linearity 測試程式**，會自動：

- 讀 exposure
    
- 計算 ADU
    
- 拟合直線
    
- 算 deviation %
    
- 輸出 linear range。