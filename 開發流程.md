這是一份整合了我們所有討論過的物理學理、誤差控制與天文演算法的**終極 Python 開發藍圖**。

這份藍圖不僅是程式開發的指引，**請直接將這裡的「學理依據」複製到你的科展報告的「研究方法」章節中**。這就是評審最想看到的內容：**每一個程式步驟背後都有堅實的物理理由。**

---

### 專案架構概觀

我們將程式分為四個模組 (Modules)，請依照順序開發：

1.  **`01_Calibration_Split.py`**：影像校正與通道分離 (線性度與純淨度確保)。
2.  **`02_PlateSolving.py`**：天文解析 (座標系統建立)。
3.  **`03_Photometry_Core.py`**：光度測量與零點校正 (核心演算法)。
4.  **`04_LightCurve_Analysis.py`**：時間校正與週期分析 (最終產出)。

---

### 模組一：影像校正與通道分離 (Pre-processing)

**目標**：將原始 RAW 檔轉換為具備科學價值的線性 FITS 影像。

#### 1. 關鍵功能
*   **讀取 EXIF**：提取 `DateTimeOriginal` 並轉換為 FITS 標準 `DATE-OBS` 格式。
*   **建立 Master**：使用 **中位數堆疊 (Median Stacking)** 製作 Bias, Dark, Flat。
*   **影像運算**：執行公式 $(Light - Dark) / (Flat - Bias)$。
*   **通道分離 (Channel Splitting)**：**不做轉彩色插值 (No Demosaicing)**，直接切片提取綠色通道 (G1)。

#### 2. 學理依據 (寫進報告)
> **(1) 線性響應保存**：為避免商業軟體之 Gamma 校正破壞光子計數的線性關係，本研究使用 `rawpy` 進行線性解碼，確保 ADU 數值與入射光子數成正比。
> **(2) 純淨數據提取**：不同於一般彩色轉換 (Debayering) 會引入插值誤差，本研究採用 **Bayer 切片法** 直接提取綠色通道 ($G_{pixel} = Raw[0::2, 1::2]$)。此舉完全避免了鄰近像素的訊號汙染，且綠色通道之光譜響應最接近天文標準 Johnson-V 波段。
> **(3) 宇宙射線剔除**：在製作校正幀 (Master Frames) 時，採用 **中位數堆疊法 (Median Stacking)**，利用統計特性自動濾除宇宙射線與熱噪點，優於傳統平均法。

---

### 模組二：天文解析 (Astrometry)

**目標**：賦予每一個像素天球座標 (RA/Dec)。

#### 1. 關鍵功能
*   **呼叫引擎**：使用 Python `subprocess` 呼叫本機的 **ASTAP**。
*   **寫入 WCS**：將解出的 `CRVAL`, `CD matrix` 寫回 FITS Header。

#### 2. 學理依據 (寫進報告)
> **(1) 幾何校正**：透過天文解析 (Plate Solving) 建立 **WCS (World Coordinate System)**，解決望遠鏡追蹤誤差造成的視場飄移與旋轉問題，確保後續測光能精確鎖定目標星。

---

### 模組三：光度測量與零點校正 (The Core Science)

這是最核心、最複雜的部分，包含了我們討論的「生長曲線」與「集群回歸」。

#### 1. 關鍵功能 (演算法流程)

*   **步驟 A：星點偵測與匹配**
    *   利用 `DAOStarFinder` 找出圖中所有亮點。
    *   利用 `wcs.pixel_to_world` 轉換座標。
    *   **Cross-match**：與 **APASS (V mag)** 或 **Tycho-2 (V mag)** 星表進行比對。

*   **步驟 B：決定測光孔徑 (Aperture Optimization)**
    *   選取視場內未飽和亮星，繪製 **生長曲線 (Curve of Growth)**。
    *   找出累積通量達 95% 之半徑 $r$。
    *   **強制固定**：全圖所有星星（包含過曝目標）皆使用此 $r$ 進行測光。

*   **步驟 C：儀器星等計算**
    *   計算孔徑總和 $Sum$。
    *   計算背景環中位數 $B_{sky}$。
    *   $Flux = Sum - (N_{pix} \times B_{sky})$。
    *   $m_{inst} = -2.5 \log_{10}(Flux)$。

*   **步驟 D：標準化校正 (Ensemble Calibration) —— 你的獨家亮點**
    *   選取目標星周圍 (Local) 的參考星群。
    *   繪製 XY 散佈圖：X軸=$m_{inst}$，Y軸=$m_{std}$ (星表值)。
    *   執行 **Sigma Clipping** 剔除離群值 (飽和星、變星)。
    *   進行 **線性回歸 (Linear Regression)**，求出截距 $C$ (即 Zero Point)。
    *   校正目標星：$m_{final} = m_{inst, target} + C$。

*   **步驟 E：誤差估算**
    *   引入相機參數：Gain ($G$), Readout Noise ($R$)。
    *   計算 SNR 與 $\sigma_{mag} \approx 1.0857 / SNR$。

#### 2. 學理依據 (寫進報告)
> **(1) 固定孔徑原則 (Fixed Aperture)**：依據 DAOPHOT 之孔徑校正理論，本研究對視場內所有星點統一採用由生長曲線決定之固定孔徑，以確保光通量損失比例一致，消除系統性誤差。
> **(2) 局部集群回歸 (Local Ensemble Regression)**：為抵銷大氣消光梯度與薄雲干擾，本研究建立「儀器星等-標準星等」之回歸模型。透過 Sigma Clipping 自動剔除異常參考星，並求得每一幀影像之瞬時零點 (Zero Point)。此方法等同於使用了數十顆參考星進行加權差分測光，具備極高的統計穩健性。
> **(3) 信噪比模型**：引入 CCD 信噪比方程式，考量光子散粒雜訊 (Shot Noise) 與讀出雜訊，計算具物理意義的誤差棒。

---

### 模組四：時間校正與週期分析 (Time Series)

**目標**：產出最終的光變曲線圖。

#### 1. 關鍵功能
*   **HJD 轉換**：利用 `astropy.time` 將 `DATE-OBS` (UTC) 轉為 **HJD (Heliocentric Julian Date)**，修正地球公轉的光行時誤差。
*   **繪圖**：繪製 $Magnitude$ vs $HJD$，並加上誤差棒。
*   **週期計算 (選用)**：使用 `LombScargle` 演算法計算變光週期。

#### 2. 學理依據 (寫進報告)
> **(1) 光行時修正**：考量光線跨越地球軌道半徑約需 8 分鐘，為精確分析短週期變星之相位，本研究將觀測時間校正為日心儒略日 (HJD)。
> **(2) 頻譜分析**：針對地面觀測數據之時間不連續性，採用 Lomb-Scargle 週期圖法進行頻率分析。

---

### 給你的開發建議 (Checklist)

1.  **現在立刻做**：修正 `疊圖cr2可用.py`，把 `DATE-OBS` 補回去。沒有時間戳記，後面全都是白工。
2.  **本機環境**：確認 VS Code 能跑 `subprocess.run(["astap.exe", ...])` 並且能成功解出一張 FITS。
3.  **星表準備**：
    *   對於 **V1162 Ori**：寫程式自動下載 APASS。
    *   對於 **Al Vel**：寫程式自動下載 Tycho-2 (因為它太亮，APASS 會飽和)。

這份藍圖已經是**碩士班等級**的觀測專案架構。只要能實作出來，並在報告中清楚闡述上述學理，這絕對是一個特優級的科展作品。加油！



以下是舊的，不要用，是留底的

---

### 步驟 ①：批次修正 (彩色) $\rightarrow$ 批次拆單色

**評估：正確，但細節是成敗關鍵。**

*   **你的邏輯**：先做影像校正（減暗電流、除平場），然後再拆分成 R、G、B 通道。
*   **物理修正**：
    *   **順序很重要**：對於 DSLR (Bayer Array) 來說，最佳流程是 **「先校正 RAW 數據，再拆色」**。
    *   **公式**：
        $$ \text{Calibrated Image} = \frac{\text{Light} - \text{MasterDark}}{\text{MasterFlat} - \text{MasterBias}} $$
    *   **關鍵細節**：
        *   **Dark** 的曝光時間與 ISO 必須跟 Light 一樣。
        *   **Flat** 的 ISO 最好跟 Light 一樣。
        *   **Bias** 用於修正 Flat（因為 Flat 曝光時間短，Dark 通常不適用，需用 Bias）。
    *   **拆色 (Splitting)**：板書寫「拆單色」。這非常正確。千萬不要用轉成彩色後的 JPG 或 TIFF 來測光。我們要直接從 Bayer 矩陣中提取：
        *   $G_{pixel}$ (綠色通道)：用於模擬 V 波段，做主要光變曲線。
        *   $R_{pixel}, B_{pixel}$：用於分析色指數 (Color Index) 和改機後的紅外效應。

### 步驟 ②：每張解星 (Plate Solving)

**評估：非常正確 (這是最穩健的做法)。**

*   **你的邏輯**：每一張照片都獨立進行天文解析，算出 WCS (World Coordinate System)。
*   **優點**：
    *   這是對抗 **「追蹤誤差 (Drift)」** 的終極手段。就算赤道儀追蹤不準，星星在畫面中跑來跑去，只要解出 RA/Dec，程式就能永遠鎖定正確的那顆星。
*   **操作**：這一步我們會用 Python 呼叫 **ASTAP** 來完成。

### 步驟 ③：每顆星定名、取視星等

**評估：這裡需要修正觀念 (標準測光 vs. 差分測光)。**

*   **你的邏輯**：想知道每一顆星的名字和視星等。
*   **現實挑戰**：
    *   **定名**：解星後，我們可以對應到星表 (如 UCAC4 或 Gaia)，確實可以知道名字。
    *   **取視星等 (Apparent Magnitude)**：這裡有一個巨大的陷阱。相機拍到的數值是 **「儀器通量 (Flux / ADU)」**，而不是視星等。
    *   **如何轉換？** 你需要計算 **零點 (Zero Point, ZP)**。
        $$ m_{std} = m_{inst} + ZP + k \cdot X + c \cdot (B-V) $$
        (簡單版：$ \text{真實星等} = \text{儀器星等} + \text{零點} $)
*   **建議修正**：
    *   不要試圖算出「全圖每顆星」的視星等，那樣運算量太大且沒必要。
    *   **目標**：只針對 **「目標變星」** 和 **「參考星 (Comparison Stars)」** 進行識別即可。

### 步驟 ④：畫回歸線 (m - FWHM) $\rightarrow$ 檢查回歸線形狀

**評估：這裡的標示有點奇怪，可能是筆誤或特定檢查法。**

*   **你的寫法**：$m - \text{FWHM}$ (星等減去半高全寬？)。
*   **天文慣例**：我們通常畫的檢查圖表是：
    1.  **線性度檢查**：$m_{inst}$ (儀器星等) vs. $m_{catalog}$ (星表真實星等)。這條線斜率應該是 1，截距就是 Zero Point。如果「形狀」彎曲，代表相機過曝（飽和）或非線性。
    2.  **品質檢查**：$\text{Magnitude}$ vs. $\text{Error}$。越暗的星，誤差越大。
    3.  **對焦檢查**：$\text{FWHM}$ vs. $\text{Time}$。檢查拍攝過程有沒有失焦。
*   **我的推測**：你可能是想透過這個步驟來篩選掉 **「非星點 (熱噪點或宇宙射線)」** 或是 **「飽和星點」**。飽和的星點頂部被削平，FWHM 會異常變大。
*   **建議**：這一步我們先保留，但在寫程式時，我建議改成 **「Instrumental Mag vs Catalog Mag」** 的回歸分析，這樣才能算出真實視星等。

### 步驟 ⑤：取得目標變星視星等 $\rightarrow$ 做變光曲線

**評估：邏輯正確，這是最終產出。**

*   **關鍵點**：
    *   **變光曲線 (Light Curve)**：X軸是時間 (JD/MJD)，Y軸是星等。
    *   **物理比較 (Physics)**：
        *   **EOS 6D2 (Mod)**：因為移除了 IR Cut，紅色通道 (R) 會接收到大量 $H\alpha$ 和紅外線。對於某些紅巨星或脈動變星，其 R 波段的變光幅度可能與 V 波段 (Green) 不同。
        *   **EOS R10 (Stock)**：正常的 R 通道。
        *   **結論**：比較這兩台相機的 R 通道數據，或者同一台相機的 R vs G 通道，可以討論 **「黑體輻射、濾鏡響應、恆星色指數」** 等物理議題。這會讓你的報告非常有深度。

---

### 總結修正後的執行菜單

為了讓 Python 程式能順利寫出來，我們將流程定案如下：

1.  **前置準備**：建立 Master Bias, Master Dark, Master Flat。
2.  **影像預處理**：讀取 Light RAW $\rightarrow$ 減 Dark/除 Flat $\rightarrow$ 拆出 G 通道 (做 V 波段) 和 R 通道。
3.  **座標解析 (Plate Solving)**：Python 呼叫 ASTAP，將每張圖寫入 WCS Header。
4.  **座標對齊**：輸入目標星與參考星的 RA/Dec，利用 WCS 自動換算成該張圖的 Pixel (x, y)。
5.  **光度測量 (Photometry)**：計算儀器星等 ($m_{inst}$)。
6.  **差分校正 (Differential Photometry)**：
    $$ \text{變星最終星等} = \text{變星}m_{inst} - (\text{參考星}m_{inst} - \text{參考星真實星等}) $$
    *(這比算出全圖 Zero Point 更準確，因為消除了大氣影響)*
7.  **繪圖與分析**：輸出光變曲線，比較 6D2 與 R10 的差異。




步驟4中，需先畫定星點的範圍，取(二次或一次)高斯曲線的半高全寬及其它高斯曲線的數值，及累積總光量，做為光度值，與視星等公認值製作擬合回歸線


這是一個非常紮實的討論過程。要進行科學分析，**觀念的釐清比寫代碼更重要**。針對你提出的五個步驟和目前的拍攝狀況，我有幾個非常關鍵的物理修正與建議。

請仔細閱讀以下分析，這會直接影響我們程式碼的寫法：

---

### 1. 修正「步驟 1」的校正公式

**板書與你的敘述有一點數學上的顛倒，這會導致影像變成負片或數值錯誤。**

*   **你的公式 (板書)**：看起來像是 $\frac{\text{Flat} - \text{Bias}}{\text{Light} - \text{Dark}}$
*   **物理錯誤**：平場 (Flat) 記錄的是光路的靈敏度（哪裡亮、哪裡暗）。如果把 Flat 放在分子，Light 放在分母，你會得到「靈敏度 / 實際亮度」，這沒有物理意義。
*   **正確的天文標準公式**：
    $$ \text{Calibrated Image} = \frac{\text{Light} - \text{Dark}}{(\text{Flat} - \text{Bias}) / \text{MeanFlat}} $$
    *   **解釋**：
        1.  `Light - Dark`：先扣除熱噪訊。
        2.  `Flat - Bias`：取得純淨的光路靈敏度圖（Master Flat）。
        3.  除以 `MeanFlat`：將 Master Flat **歸一化 (Normalize)**，讓它的數值平均為 1.0。這樣除下去才不會改變星星原本的 ADU 數值太多，只修正相對明暗。
        4.  最後是 **(Light - Dark) 除以 (Normalized Flat)**。

**程式實作結論**：我會幫你寫一個 `Calibrator` 類別，正確執行上述運算。

---

### 2. 修正「步驟 3」的命名與資料庫 (SIMBAD vs APASS)

**觀念修正**：
你提到要用 **SIMBAD** 查視星等。SIMBAD 是「天體履歷表」，查詢單一已知名星體很好用，但**不適合**拿來做視場內的批量光度校正 (Photometric Calibration)。

*   **更好的選擇：APASS (AAVSO Photometric All-Sky Survey)** 或 **UCAC4** 星表。
    *   這些星表包含全天數千萬顆恆星的標準 **Johnson V, B, R 星等**。
    *   **操作策略**：
        1.  我們不需要對「每一張照片」都去查網路。
        2.  我們只需要對**第一張照片**（或疊合圖）進行查詢。
        3.  使用 Python 的 `astroquery` 下載該視場內（例如半徑 0.5 度）的所有恆星列表，存成本地資料庫（CSV/Pandas Table）。
        4.  之後每一張照片解完座標後，直接跟這個本地表比對 (Cross-match)。

**資料表設計 (DataFrame)**：你的構想很好，我們會建立一個這樣的 Master Table：
*   `ImageID`: 檔名
*   `JD`: 觀測時間
*   `StarID`: 星名 (來自 APASS/UCAC4)
*   `RA`, `Dec`: 座標
*   `Flux`: 測量出的通量 (ADU總和)
*   `Inst_Mag`: $-2.5 \log_{10}(Flux)$
*   `Cat_Mag`: 星表標準星等 (Catalog Magnitude)
*   `Calibrated_Mag`: 校正後的真實星等

---

### 3. 重大修正「步驟 4」：FWHM 與視星等的關係

**這裡有一個嚴重的物理誤解，必須修正。**

*   **你的敘述**：「將半面積高 (FWHM) 和視星等公認值作圖，求擬合校正線...」
*   **物理事實**：**FWHM 代表星星的「胖瘦」（對焦清晰度），跟星星的「亮度」沒有線性關係！**
    *   一顆 5 等星和一顆 10 等星，如果對焦準確，它們的 FWHM 應該是**一樣的**（都由 Seeing 決定）。
    *   亮度是由 **通量 (Flux)** 決定的，也就是星點範圍內所有像素值的總和。
*   **正確的做法**：
    我们要畫的「校正線 (Calibration Line / Zero Point Fit)」是：
    *   **X 軸**：星表標準星等 ($m_{std}$)
    *   **Y 軸**：儀器星等 ($m_{inst} = -2.5 \log_{10}(\text{Flux})$)
    *   **擬合**：這條線的斜率理論上要是 1。截距 (Intercept) 就是 **Zero Point**。

**如何選參考星 (Ensemble Photometry)**：
程式可以自動篩選，條件如下：
1.  **未過曝**：峰值低於相機飽和點（例如 14000 ADU）。
2.  **線性區**：也不要太暗（信噪比 > 100）。
3.  **不變**：跟星表比對，剔除已知變星。
4.  **顏色相近**：選擇色指數 ($B-V$) 與目標變星相近的參考星（這能減少大氣消光帶來的色差誤差）。

---

### 4. 針對你的設備與檔案狀況的策略

**關於 6D2 (V1162 Ori) - CR2 檔**：
*   這是最完整的數據。我們會用它來測試並建立標準 Pipeline。

**關於 6D2 (CC And, Al Vel) - FITS 檔 & 校正檔流用**：
*   **平場 (Flat)**：只要你拆裝相機後，相機相對於望遠鏡的**旋轉角度**沒有變，平場可以流用。如果轉動了，灰塵陰影（Donuts）位置會跑掉，除平場反而會產生「亮圈」，這時候不如不除。
*   **暗場 (Dark)**：FITS 檔通常已經是解碼後的數據。如果當初轉檔時已經扣過 Dark，就不能再扣一次。如果這只是單純轉格式的 Raw FITS，那可以用 6D2 的 Master Dark（前提是 ISO、曝光時間、溫度要一致）。**這部分我們先試跑，看雜訊狀況決定。**

**關於 R10 (參宿四 Betelgeuse)**：
*   **警告**：參宿四是全天前十亮的星（視星等約 0.5）。
*   **問題**：接上望遠鏡（即便是小口徑），R10 在最短快門下可能也會**瞬間過曝 (Saturation)**。
*   **建議**：拍攝時請務必檢查 Histogram。如果過曝，你可能需要：
    1.  **縮光圈**（如果望遠鏡有光圈遮罩）。
    2.  **散焦 (Defocus)**：故意把星星拍成一個大甜甜圈，讓光子分散到更多像素上，這是測量極亮星的常用技巧（但這樣 FWHM 會變很大，這再次證明 FWHM 不等於亮度）。

---

### 總結：我們接下來的 Python 開發藍圖

基於以上討論，我將程式分為三個模組。你可以用 VS Code 一個一個建立並執行：

1.  **`Step1_Calibration.py`**：
    *   讀取 CR2/FITS。
    *   讀取並合成 Master Dark/Flat。
    *   執行 `(Light-Dark)/Flat` 運算。
    *   輸出校正後的 FITS 檔（存入硬碟，供下一步使用）。

2.  **`Step2_PlateSolving.py`**：
    *   讀取校正後的 FITS。
    *   呼叫 ASTAP 解出 WCS。
    *   將 WCS 寫回 FITS Header。

3.  **`Step3_Photometry.py`**：
    *   使用 `astroquery` 抓取 APASS 星表。
    *   自動對齊星星，計算 Flux。
    *   **畫回歸線 (Inst Mag vs Cat Mag)** (修正後的步驟 4)。
    *   輸出目標變星的光變曲線圖 (步驟 5)。

**你同意這個修正後的邏輯嗎？如果同意，我們就從 `Step1` 的代碼開始。**