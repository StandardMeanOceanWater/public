# 回歸線
現在是「自由斜率 + 有加權 + 迭代剔除」回歸。

**回歸模型**
每一幀用比較星做：

1. `m_inst = a * m_cat + b`
2. 再把目標星換成視星等：
   `m_var = (m_inst_t - b) / a`

對應在 [photometry.py:2266](M:\pipeline\photometry.py:2266)。

**權重怎麼來**
每顆比較星的初始權重是：

1. 距離權重：
   `weight = 1 / (dist_arcsec + epsilon)^2`
2. 若有星表誤差 `m_err`：
   `weight /= m_err^2`

對應在 [photometry.py:2168](M:\pipeline\photometry.py:2168) 到 [photometry.py:2172](M:\pipeline\photometry.py:2172)。

之後把 `comp_weights` 傳進回歸函式，在 [photometry.py:2198](M:\pipeline\photometry.py:2198) 到 [photometry.py:2203](M:\pipeline\photometry.py:2203)。

**回歸本體怎麼跑**
在 [robust_linear_fit](M:\pipeline\phot_regression.py:20)：

1. 先用 HuberRegressor（`epsilon=1.35`）擬合
2. 權重會以 `sqrt(w)` 當 `sample_weight` 傳入（[phot_regression.py:67](M:\pipeline\phot_regression.py:67), [phot_regression.py:73](M:\pipeline\phot_regression.py:73)）
3. 再做迭代 sigma clipping：
   - 殘差 `resid = m_inst - (a*m_cat + b)`
   - 保留 `|resid| <= sigma * std`
   - 重複到收斂或到 `max_iter`
   對應 [phot_regression.py:84](M:\pipeline\phot_regression.py:84) 到 [phot_regression.py:95](M:\pipeline\phot_regression.py:95)
4. 最後在 inlier 子集上算 `R²`（[phot_regression.py:97](M:\pipeline\phot_regression.py:97) 到 [phot_regression.py:103](M:\pipeline\phot_regression.py:103)）

**一句話總結**
目前目標星視星等不是固定 `slope=1` 的純差分；而是每幀用比較星做「有權重的 robust 線性回歸」，再用該幀的 `a,b` 把目標星轉成 `m_var`。

# 誤差線
用每一幀的 `t_sigma_mag`（1-sigma 星等誤差）畫的。

1. 計算入口在 [mag_error_from_flux](M:\pipeline\phot_regression.py:113)  
2. 測光主流程呼叫在 [photometry.py](M:\pipeline\photometry.py:2279)  
3. 存入欄位在 [photometry.py](M:\pipeline\photometry.py:2306)（`"t_sigma_mag": sigma_mag`）  
4. 繪圖時優先用 `t_sigma_mag`，沒有才退回 `v_err`，在 [polt_light_curve.py](M:\pipeline\tools\polt_light_curve.py:445) 到 [polt_light_curve.py](M:\pipeline\tools\polt_light_curve.py:449)

實際公式（程式實作）是 CCD noise equation 轉星等誤差：

1. 先轉電子數：`f_e = flux_net * gain`
2. 背景修正：`sky_correction = 1 + n_pix/n_sky`（若 `n_sky` 缺失就用 `1.0`）
3. 背景標準差轉電子：`bstd_e = b_sky_std * gain`
4. 總通量噪聲：
   `sigma_flux_e = sqrt( f_e + n_pix*sky_correction*(bstd_e^2 + rn^2) )`
5. 星等誤差：
   `sigma_mag = 1.0857 * sigma_flux_e / f_e`

對應程式在 [phot_regression.py](M:\pipeline\phot_regression.py:149) 到 [phot_regression.py](M:\pipeline\phot_regression.py:172)。

補充兩點：

1. 若 `gain` 沒有、`flux_net <= 0`、或 `n_pix <= 0`，函式會回傳 `NaN`，該點通常不會有有效誤差線（見 [phot_regression.py](M:\pipeline\phot_regression.py:142) 到 [phot_regression.py](M:\pipeline\phot_regression.py:147)）。  
2. 程式已改成用 `b_sky_std`（背景標準差），不是背景中位數。


# 傅立葉怎麼控制的
Breger S/N 選階是指：用 **Breger 的頻譜信噪比標準** 來決定傅立葉模型要加到第幾階（幾個頻率/諧波）。

簡單說法：
1. 先做頻率分析，找到一個候選頻率峰。
2. 算它的振幅 `A_peak`。
3. 在附近「沒有訊號的背景區」估計噪聲振幅 `A_noise`。
4. 算 `S/N = A_peak / A_noise`。
5. 若 `S/N >= 4`（Breger 常用門檻），就保留這個成分；否則當成雜訊，不再往更高階加。

所以「選階」本質是：  
- 加新階後若新成分還有足夠顯著（S/N 過門檻）就繼續。  
- 一旦不顯著就停止，避免過度擬合。  

如果你要，我可以下一步直接對你目前 `period_analysis.py` 的實作逐行對照：它的噪聲窗怎麼取、是不是固定門檻 4、以及最後是怎麼決定停止階數。
你這套程式裡，**沒有「貝葉斯模型去控制傅立葉係數」**（沒有 posterior / MCMC / Bayes update）。

你說的「貝葉斯」其實是指 **BIC**（Bayesian Information Criterion，貝葉斯資訊準則）這個「選模型階數」指標。

目前實際控制流程在 [period_analysis.py](M:\pipeline\period_analysis.py) 是：

1. 先用 LS 找最佳週期 `best_period`（[period_analysis.py:1281](M:\pipeline\period_analysis.py:1281)）
2. 進 Fourier 擬合時，要決定諧波數 `N`
3. 原始設計有 BIC 函式（檔內註解也寫 BIC）
4. 但現在主路徑實際呼叫的是 **Breger S/N 選階**（[period_analysis.py:652](M:\pipeline\period_analysis.py:652) 到 [period_analysis.py:656](M:\pipeline\period_analysis.py:656)）
5. 選到 `best_n` 後，再固定 `N=best_n` 做兩步 Fourier 擬合（[period_analysis.py:667](M:\pipeline\period_analysis.py:667) 之後）
6. 最後輸出 `n_harmonics`（[period_analysis.py:717](M:\pipeline\period_analysis.py:717)）

所以精確講法是：

1. 不是「Bayesian 後驗」在控 Fourier
2. 是「模型選階準則（BIC/Breger）」在控 `n_harmonics`
3. 而你目前程式的實際主路徑偏向 **Breger S/N 選階**，不是純 BIC pipeline

貝葉斯分析的核心是：

`後驗機率 ∝ 先驗機率 × 似然`

也就是：

1. `先驗 (Prior)`：你在看資料前，對參數的原本看法。  
2. `似然 (Likelihood)`：這組參數產生目前資料的可能性。  
3. `後驗 (Posterior)`：看完資料後，更新過的看法。  

直觀版：
- 你原本「覺得某參數大概在某範圍」。
- 新資料進來後，會把不合理的範圍壓低、合理的範圍拉高。
- 最後得到一整條機率分布（不只是一個單點答案）。

常見用途：
- 參數估計（含不確定度區間）
- 模型比較（例如 Bayes factor）
- 可把先驗知識（儀器限制、物理範圍）正式放進計算

如果你要，我可以直接用你們變星流程舉一個「週期/振幅」的貝葉斯範例，寫成最小可算的公式版本。
---
以上是我們家才是這樣算的東西
以下是業界都一樣的東西
# 儀器星等（instrumental magnitude）
這樣算的：

1. 先算目標星淨通量 `flux_net`（孔徑總和扣背景）
2. 再算  
   `m_inst = -2.5 * log10(flux_net)`

這個定義在你程式的測光流程裡就是標準天文定義。你可在 [photometry.py](M:\pipeline\photometry.py) 搜 `t_m_inst` 與 `flux_net` 的計算路徑看到。

為什麼會是負值：

1. 當 `flux_net > 1` 時，`log10(flux_net)` 是正數
2. 前面有負號 `-2.5`
3. 所以 `m_inst` 會變負

這是正常的，不是錯誤。  
`m_inst` 本來就是「相對量」，零點是任意的，重點是不同幀之間的差值和回歸關係，不是它絕對值是不是負數。

直觀例子：

1. `flux_net = 10000`
2. `log10(10000)=4`
3. `m_inst = -2.5*4 = -10`

所以看到 `-8`, `-10`, `-12` 這類值都很常見。  
之後程式會用比較星回歸把它轉到實際星等系統（`m_var`），不是直接把 `m_inst` 當最終物理星等。

# `FLUX`是用孔徑法算的：

1. 先取目標孔徑內像素總和：`sum_ap = cut[ap_mask].sum()`
2. 用背景環像素中位數當每像素背景：`b_sky = median(ann_pixels)`
3. 背景扣除後淨通量：`flux_net = sum_ap - b_sky * n_pix`

對應程式在 [phot_core.py](M:\pipeline\phot_core.py:137) 到 [phot_core.py](M:\pipeline\phot_core.py:175)，核心行是 [phot_core.py:165](M:\pipeline\phot_core.py:165)。

主測光流程就是讀這個 `flux_net`，在 [photometry.py:2062](M:\pipeline\photometry.py:2062) 到 [photometry.py:2072](M:\pipeline\photometry.py:2072) 可以看到：

1. `phot_t = aperture_photometry(...)`
2. `m_inst_t = m_inst_from_flux(phot_t["flux_net"])`

補充兩點：

1. `b_sky_std`（背景標準差）也會算，但它是給誤差方程用，不是用來扣背景（見 [phot_core.py:164](M:\pipeline\phot_core.py:164) 與 [phot_regression.py:113](M:\pipeline\phot_regression.py:113)）。
2. 程式中另有高斯擬合版本的 `flux_net = A * 2πσxσy`（見 [phot_core.py:67](M:\pipeline\phot_core.py:67)），主要是星像參數估計路徑，不是你主光變曲線每幀的主要 `flux_net` 來源。

`ADU` 常見中文是：

1. `類比數位單位`（最常用）
2. `模數單位`（較少見）

英文全名是 `Analog-to-Digital Unit`。